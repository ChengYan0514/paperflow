# Domain Documentation

This is a single-context repository. Before exploring a feature, read the root `CONTEXT.md` and any relevant decisions under `docs/adr/`.

Use the glossary's established vocabulary in test names, issue titles, API descriptions, and implementation notes. If an intended change conflicts with an ADR, surface that conflict rather than silently overriding the decision.
